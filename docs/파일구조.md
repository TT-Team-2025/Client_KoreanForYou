# Front_KoreanForYou 프로젝트 파일 구조

> **프로젝트명**: Front_KoreanForYou (클라이언트)
> **설명**: 요식업 외국인 근로자를 위한 실무 한국어 학습 플랫폼
> **Node 버전**: 20 사용 권장

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [디렉토리 구조](#디렉토리-구조)
4. [주요 파일 상세 설명](#주요-파일-상세-설명)
5. [컴포넌트 구조](#컴포넌트-구조)
6. [아키텍처 패턴](#아키텍처-패턴)
7. [주요 기능 플로우](#주요-기능-플로우)
8. [주요 명령어](#주요-명령어)

---

## 🎯 프로젝트 개요

Front_KoreanForYou는 요식업 외국인 근로자들이 실무에서 필요한 한국어를 효과적으로 학습할 수 있도록 돕는 웹 애플리케이션입니다.

**핵심 기능**:
- 📚 레벨별 문장 학습 (초급/중급/고급)
- 🎤 발음 연습 및 피드백
- 🤖 AI 기반 대화 시뮬레이션
- 📊 학습 진도 추적 및 통계
- 👥 커뮤니티 기능 (게시판, 댓글)

---

## 🛠 기술 스택

### Core
- **React** 19.1.1 + **TypeScript**
- **Vite** 7.1.7 (빌드 도구)

### UI/스타일링
- **Tailwind CSS** 4.1.3
- **Radix UI** (헤드리스 컴포넌트 라이브러리)
- **Lucide React** (아이콘)
- **Class Variance Authority** (조건부 스타일링)

### 폼 & 상태
- **React Hook Form** 7.65.0
- **Next Themes** (다크 모드)

### 차트 & 시각화
- **Recharts** 3.3.0

### 기타
- **Embla Carousel** (캐러셀)
- **Sonner** (토스트 알림)
- **Vaul** (드로어)

---

## 📁 디렉토리 구조

```
Front_KoreanForYou/
├── src/                          # 소스코드 디렉토리
│   ├── components/              # React 컴포넌트
│   │   ├── [화면 컴포넌트들]    # 23개의 주요 화면 컴포넌트
│   │   ├── ui/                 # 48개의 재사용 가능한 UI 컴포넌트
│   │   ├── layout/             # 레이아웃 관련 컴포넌트
│   │   ├── shared/             # 여러 컴포넌트에서 공유하는 컴포넌트
│   │   ├── figma/              # Figma 디자인 연동 컴포넌트
│   │   └── Navigation.tsx      # 전역 네비게이션 바
│   ├── types/                   # TypeScript 타입 정의
│   │   └── index.ts            # 209줄의 타입 정의
│   ├── constants/               # 상수 정의
│   │   ├── levels.ts           # 레벨 정보 상수
│   │   └── jobs.ts             # 직무 정보 상수
│   ├── styles/                  # 전역 스타일
│   │   └── globals.css         # Tailwind 커스텀 변수 및 테마
│   ├── assets/                  # 이미지, 폰트 등 정적 자산
│   ├── App.tsx                 # 메인 애플리케이션 컴포넌트 (187줄)
│   ├── main.tsx                # React DOM 진입점
│   └── index.css               # Tailwind CSS 전체 프레임워크
├── public/                       # 정적 파일
├── docs/                         # 프로젝트 문서
│   ├── 파일구조.md              # 이 문서
│   ├── 커밋규칙.md
│   ├── 역할분담.md
│   └── 2025-10-25-수정사항.md
├── index.html                    # HTML 템플릿
├── package.json                  # 의존성 및 스크립트
├── vite.config.js               # Vite 설정
├── eslint.config.js             # ESLint 설정
└── .gitignore                    # Git 무시 파일
```

---

## 📄 주요 파일 상세 설명

### 루트 설정 파일

#### `package.json`
프로젝트 정보 및 의존성 관리 파일

**주요 스크립트**:
```json
{
  "dev": "vite",           // 개발 서버
  "build": "vite build",   // 프로덕션 빌드
  "lint": "eslint .",      // 린트 체크
  "preview": "vite preview" // 빌드 미리보기
}
```

#### `vite.config.js`
Vite 빌드 도구 설정 파일
- React Fast Refresh 지원
- 모듈 번들링 설정

#### `index.html`
애플리케이션의 진입점 HTML 파일
- React 앱이 마운트될 `<div id="root">` 포함
- `src/main.tsx`를 모듈로 로드

#### `.gitignore`
Git에서 제외할 파일/폴더 목록
- `node_modules/`, `dist/`, 로그 파일 등

---

### src 디렉토리

#### `main.tsx` (7줄)
React 애플리케이션의 진입점
```typescript
createRoot(document.getElementById("root")!).render(<App />);
```

#### `App.tsx` (187줄)
메인 애플리케이션 컴포넌트 - **프로젝트의 핵심 상태 관리**

**주요 역할**:
- 23개 화면 간 네비게이션 관리
- 사용자 데이터 상태 관리
- 학습 기록 관리
- 화면 전환 히스토리 추적

**관리하는 화면 타입**:
```typescript
type Screen =
  | 'landing' | 'login' | 'signup' | 'home'
  | 'levelTest' | 'levelTestResult' | 'levelUp'
  | 'chapterList' | 'sentenceLearning' | 'pronunciation'
  | 'scenarioSelect' | 'conversationSetup' | 'conversation'
  | 'feedback' | 'progress' | 'community'
  | 'postDetail' | 'postCreate' | 'mypage'
  | 'passwordChange' | 'languageSettings'
  | 'myPosts' | 'myComments'
```

**핵심 상태**:
- `currentScreen`: 현재 화면
- `navigationHistory`: 네비게이션 히스토리
- `userData`: 사용자 정보 (닉네임, 직무, 레벨)
- `conversationSetup`: AI 대화 설정
- `selectedChapter`: 선택된 챕터

**핵심 함수**:
- `handleNavigate()`: 화면 전환
- `handleBack()`: 뒤로 가기
- `handleStartConversation()`: AI 대화 시작
- `handleSelectChapter()`: 챕터 선택

#### `index.css` (35,500+ 줄)
Tailwind CSS 전체 프레임워크 포함
- 컬러 팔레트
- 반응형 그리드
- 모든 유틸리티 클래스

---

## 🧩 컴포넌트 구조

### 컴포넌트 계층도

```
UI 컴포넌트 (ui/)
  ↑
Shared 컴포넌트 (shared/)
  ↑
Layout 컴포넌트 (layout/)
  ↑
화면 컴포넌트 (Screen 컴포넌트들)
  ↑
App.tsx
```

### 1. 화면(Screen) 컴포넌트 (23개)

모든 화면 컴포넌트는 `src/components/` 디렉토리에 위치

| 컴포넌트명 | 파일 | 역할 | 크기 |
|-----------|------|------|------|
| LandingPage | `LandingPage.tsx` | 랜딩/메인 페이지 | 347줄 |
| LoginScreen | `LoginScreen.tsx` | 로그인 화면 | 71줄 |
| SignupScreen | `SignupScreen.tsx` | 회원가입 화면 | 126줄 |
| HomeScreen | `HomeScreen.tsx` | 대시보드/홈 화면 | 250줄 |
| LevelTestScreen | `LevelTestScreen.tsx` | 레벨 테스트 | 92줄 |
| LevelTestResultScreen | `LevelTestResultScreen.tsx` | 레벨 테스트 결과 | 69줄 |
| LevelUpScreen | `LevelUpScreen.tsx` | 레벨 업 축하 화면 | 167줄 |
| ChapterListScreen | `ChapterListScreen.tsx` | 챕터 목록 화면 | 430줄 |
| SentenceLearningScreen | `SentenceLearningScreen.tsx` | 문장 학습 화면 (가장 큼) | 1,325줄 |
| PronunciationScreen | `PronunciationScreen.tsx` | 발음 연습 화면 | 169줄 |
| ScenarioSelectScreen | `ScenarioSelectScreen.tsx` | 시나리오 선택 화면 | 137줄 |
| ConversationSetupScreen | `ConversationSetupScreen.tsx` | AI 대화 설정 화면 | 146줄 |
| ConversationScreen | `ConversationScreen.tsx` | AI 대화 진행 화면 | 224줄 |
| FeedbackScreen | `FeedbackScreen.tsx` | 피드백/결과 화면 | 387줄 |
| ProgressScreen | `ProgressScreen.tsx` | 학습 진도 화면 | 415줄 |
| CommunityScreen | `CommunityScreen.tsx` | 커뮤니티 화면 | 248줄 |
| PostDetailScreen | `PostDetailScreen.tsx` | 게시물 상세 화면 | 180줄 |
| PostCreateScreen | `PostCreateScreen.tsx` | 게시물 작성 화면 | 110줄 |
| MyPageScreen | `MyPageScreen.tsx` | 마이페이지 | 281줄 |
| MyPostsScreen | `MyPostsScreen.tsx` | 내 게시물 | 159줄 |
| MyCommentsScreen | `MyCommentsScreen.tsx` | 내 댓글 | 168줄 |
| PasswordChangeScreen | `PasswordChangeScreen.tsx` | 비밀번호 변경 | 189줄 |
| LanguageSettingsScreen | `LanguageSettingsScreen.tsx` | 언어 설정 | 121줄 |$

**관련 컴포넌트**:
- `Navigation.tsx` (90줄): 전역 네비게이션 바
- `StudyCalendar.tsx` (243줄): 학습 캘린더

**전체 화면 컴포넌트 총 줄 수**: 6,144줄

---

### 2. UI 컴포넌트 디렉토리 (`src/components/ui/`)

**48개의 재사용 가능한 UI 컴포넌트** - Radix UI 기반

#### 기본 컴포넌트
- `button.tsx` - 버튼
- `input.tsx` - 텍스트 입력
- `textarea.tsx` - 텍스트 영역
- `label.tsx` - 레이블
- `badge.tsx` - 배지
- `card.tsx` - 카드 컨테이너
- `alert.tsx` - 알림/경고
- `separator.tsx` - 구분선

#### 폼/입력 관련
- `form.tsx` - React Hook Form 통합
- `input-otp.tsx` - OTP 입력
- `checkbox.tsx` - 체크박스
- `radio-group.tsx` - 라디오 버튼
- `select.tsx` - 선택 드롭다운
- `switch.tsx` - 토글 스위치
- `slider.tsx` - 슬라이더
- `toggle.tsx`, `toggle-group.tsx` - 토글 버튼

#### 다이얼로그/모달
- `dialog.tsx` - 모달 다이얼로그
- `alert-dialog.tsx` - 알림 다이얼로그
- `drawer.tsx` - 사이드 드로어
- `popover.tsx` - 팝오버
- `hover-card.tsx` - 호버 카드
- `sheet.tsx` - 시트 패널

#### 네비게이션
- `tabs.tsx` - 탭
- `pagination.tsx` - 페이지네이션
- `breadcrumb.tsx` - 브레드크럼
- `navigation-menu.tsx` - 네비게이션 메뉴
- `menubar.tsx` - 메뉴바
- `dropdown-menu.tsx` - 드롭다운 메뉴
- `context-menu.tsx` - 우클릭 컨텍스트 메뉴
- `accordion.tsx` - 어코디언
- `collapsible.tsx` - 접을 수 있는 섹션

#### 시각화/고급
- `chart.tsx` - 차트 (Recharts 통합)
- `carousel.tsx` - 캐러셀 (Embla Carousel)
- `scroll-area.tsx` - 스크롤 영역
- `resizable.tsx` - 리사이즈 가능한 패널
- `sidebar.tsx` - 사이드바
- `skeleton.tsx` - 로딩 스켈레톤
- `aspect-ratio.tsx` - 종횡비 유지
- `avatar.tsx` - 아바타
- `progress.tsx` - 진행률 바
- `tooltip.tsx` - 툴팁
- `command.tsx` - 커맨드 팔레트
- `sonner.tsx` - 토스트 알림
- `table.tsx` - 테이블

#### 유틸리티
- `utils.ts` - 클래스 병합 유틸리티 (clsx, tailwind-merge)
- `use-mobile.ts` - 모바일 감지 Hook

---

### 3. Layout 컴포넌트 (`src/components/layout/`)

#### `ScreenHeader.tsx` (41줄)
모든 화면의 공통 헤더 컴포넌트

**지원 기능**:
- 뒤로 가기 버튼
- 화면 제목 및 부제목
- 우측 액션 버튼

---

### 4. Shared 컴포넌트 (`src/components/shared/`)

재사용 가능한 비즈니스 로직 컴포넌트

| 컴포넌트 | 파일 | 역할 | 크기 |
|---------|------|------|------|
| SentenceCard | `SentenceCard.tsx` | 문장 카드 (한/영, 유의어, 오디오/녹음) | 66줄 |
| ConversationBubble | `ConversationBubble.tsx` | 대화 말풍선 (사용자/AI 구분) | 36줄 |
| LevelBadge | `LevelBadge.tsx` | 레벨 배지 (초급/중급/고급) | 23줄 |
| JobBadge | `JobBadge.tsx` | 직무 배지 | 16줄 |
| StatCard | `StatCard.tsx` | 통계 카드 | 24줄 |
| QuickAccessCard | `QuickAccessCard.tsx` | 빠른 접근 카드 | 41줄 |
| LearningRecordCard | `LearningRecordCard.tsx` | 학습 기록 카드 | 52줄 |

---

### 5. Figma 컴포넌트 (`src/components/figma/`)

#### `ImageWithFallback.tsx` (28줄)
Figma 이미지 폴백 처리 컴포넌트
- 이미지 로드 실패 시 기본 이미지 표시

---

### 6. Navigation 컴포넌트

#### `Navigation.tsx` (90줄)
전역 네비게이션 바

**기능**:
- 로고 및 메인 네비게이션 아이템 (5개)
- 사용자 메뉴
- 반응형 디자인 (데스크톱 + 모바일)

**네비게이션 항목**:
1. 홈 (Home)
2. 문장 학습 (BookOpen)
3. AI 연습 (MessageSquare)
4. 학습 기록 (BarChart3)
5. 커뮤니티 (Users)

---

## 📦 Types & Constants

### `src/types/index.ts` (209줄)

TypeScript 타입 정의 모음

#### 주요 타입

**레벨 시스템**:
```typescript
interface MemberLevel {
  lev_id: number;              // 1=초급, 2=중급, 3=고급
  level_name_ko: string;
  level_name_en?: string;
  passing_rule?: {
    min_pron: number;
    wpm: [number, number];
  };
  sort_order: number;
}
```

**회원 정보**:
```typescript
interface Member {
  member_id: number;
  nickname: string;
  email: string;
  job: string;                 // 직무 코드
  level_id: number;            // 현재 레벨
  created_at: string;
}
```

**챕터 정보**:
```typescript
interface Chapter {
  chapter_id: string;
  chapter_name: string;
  chapter_type: 'common' | 'job';  // 공통 or 직무별
  level_id: number;
  order_num: number;
  // 프론트 계산 필드
  is_locked?: boolean;
  progress?: number;
}
```

**문장 정보**:
```typescript
interface Sentence {
  sentence_id: number;
  chapter_id: string;
  text_ko: string;
  text_en: string;
  text_vi?: string;
  tts_url?: string;
  order_num: number;
  mastery?: number;            // 숙련도 (0-100)
}
```

**피드백**:
```typescript
interface Feedback {
  feedback_id: number;
  member_id: number;
  context_type: 'scenario' | 'chapter' | 'sentence';
  pron_score?: number;         // 발음 점수
  fluency_score?: number;      // 유창성
  intonation_score?: number;   // 억양
  comment?: string;
  created_at: string;
}
```

**학습 기록 (UI용 통합 타입)**:
```typescript
interface LearningRecord {
  id: number | string;
  type: 'conversation' | 'sentence';
  title: string;
  date: string;
  score?: number;
  duration?: string;
  progress?: number;
}
```

#### 헬퍼 함수
- `isChapterUnlocked(userLevelId, chapterLevelId)` - 챕터 잠금 해제 확인
- `getLevelName(levelId)` - 레벨명 조회

#### 맵(Map)
- `LEVEL_MAP` - 레벨 정보 맵
- `JOB_MAP` - 직무 정보 맵

---

### `src/constants/levels.ts` (25줄)

레벨 관련 상수 정의

```typescript
export const LEVEL_INFO = {
  1: {
    name: '초급',
    description: '기본적인 한국어 인사와 간단한 문장을 익힙니다.',
    color: 'bg-green-100 text-green-700',
    icon: '🌱'
  },
  2: {
    name: '중급',
    description: '실무에서 자주 사용하는 표현을 학습합니다.',
    color: 'bg-blue-100 text-blue-700',
    icon: '🌿'
  },
  3: {
    name: '고급',
    description: '복잡한 상황에서 능숙하게 대응합니다.',
    color: 'bg-purple-100 text-purple-700',
    icon: '🌳'
  }
}
```

---

### `src/constants/jobs.ts` (42줄)

직무 관련 상수 정의

```typescript
export const JOB_INFO = {
  'kitchen-assistant': {
    name: '주방보조',
    emoji: '👨‍🍳',
    color: 'bg-orange-100 text-orange-700'
  },
  'server': {
    name: '서빙',
    emoji: '🍽️',
    color: 'bg-blue-100 text-blue-700'
  },
  'barista': {
    name: '바리스타',
    emoji: '☕',
    color: 'bg-amber-100 text-amber-700'
  },
  'cashier': {
    name: '캐셔',
    emoji: '💰',
    color: 'bg-green-100 text-green-700'
  },
  'delivery': {
    name: '배달',
    emoji: '🚴',
    color: 'bg-purple-100 text-purple-700'
  },
  'head-chef': {
    name: '주방장',
    emoji: '👨‍🍳',
    color: 'bg-red-100 text-red-700'
  },
  'dishwasher': {
    name: '설거지',
    emoji: '🧽',
    color: 'bg-cyan-100 text-cyan-700'
  }
}
```

---

## 🎨 Styles

### `src/styles/globals.css` (190줄)

전역 스타일 및 테마 설정

**주요 내용**:
- Tailwind CSS v4 커스텀 변수 정의
- 라이트/다크 모드 CSS 변수
- 색상 변수들 (primary, secondary, accent, destructive 등)
- 차트 색상 변수
- 사이드바 테마 변수
- 기본 타이포그래피 스타일

---

## 🏗 아키텍처 패턴

### 1. 상태 관리 패턴

**Props Drilling 기반**:
- [App.tsx:1-187](src/App.tsx#L1-L187)에서 모든 상태 관리
- 자식 컴포넌트로 props 전달
- 콜백 함수를 통한 상태 업데이트

```
App (상태 관리)
  ├── Navigation (전역 네비게이션)
  ├── [현재 화면 컴포넌트]
  └── Toaster (토스트 알림)
```

**향후 개선 방향**:
- Redux/Zustand/Context API로 전환 권장
- 대규모 상태 관리 필요 시 고려

---

### 2. 라우팅 방식

**상태 기반 라우팅** (URL 라우팅 없음):
- SPA 클라이언트 사이드 라우팅
- `currentScreen` 상태로 화면 렌더링

```typescript
renderScreen() {
  switch (currentScreen) {
    case 'landing': return <LandingPage ... />
    case 'login': return <LoginScreen ... />
    case 'home': return <HomeScreen ... />
    // ... 23개의 화면
  }
}
```

**향후 개선 방향**:
- React Router 도입 권장
- URL 기반 네비게이션으로 전환

---

### 3. 데이터 흐름

```
사용자 상호작용
  ↓
화면 컴포넌트의 onClick 핸들러
  ↓
App의 handleNavigate() 호출
  ↓
상태 업데이트 (currentScreen, data 등)
  ↓
재렌더링 → 새 화면 표시
```

---

### 4. 스타일링 접근 방식

**Tailwind CSS + 컴포넌트 기반**:
- 모든 스타일을 Tailwind 클래스로 구성
- UI 컴포넌트에 `className` props 전달
- `clsx` 또는 `classNames`로 조건부 스타일링

```typescript
<Button
  className={`text-${color} bg-${bgColor} ${isActive ? 'opacity-100' : 'opacity-50'}`}
/>
```

---

### 5. 데이터 소스

**현재: Mock 데이터**
- [App.tsx](src/App.tsx)의 userData
- [SentenceLearningScreen.tsx](src/components/SentenceLearningScreen.tsx)의 chapterSentences
- [ChapterListScreen.tsx](src/components/ChapterListScreen.tsx)의 commonChapters, jobSpecificChapters
- [HomeScreen.tsx](src/components/HomeScreen.tsx)의 recentRecords

**향후: API 연동 예상**
- Flask/Django 백엔드와의 REST API
- TTS 오디오 스트리밍
- 피드백 데이터 POST
- 실시간 학습 기록 동기화

---

## 🔄 주요 기능 플로우

### 1. 인증 플로우

```
LandingPage
  → LoginScreen 또는 SignupScreen
    → HomeScreen (메인 대시보드)
```

**관련 파일**:
- [LandingPage.tsx:1-347](src/components/LandingPage.tsx#L1-L347)
- [LoginScreen.tsx:1-71](src/components/LoginScreen.tsx#L1-L71)
- [SignupScreen.tsx:1-126](src/components/SignupScreen.tsx#L1-L126)
- [HomeScreen.tsx:1-250](src/components/HomeScreen.tsx#L1-L250)

---

### 2. 문장 학습 플로우

```
HomeScreen
  → ChapterListScreen (챕터 선택)
    → SentenceLearningScreen (문장 학습)
      → FeedbackScreen (피드백)
```

**관련 파일**:
- [ChapterListScreen.tsx:1-430](src/components/ChapterListScreen.tsx#L1-L430)
- [SentenceLearningScreen.tsx:1-1325](src/components/SentenceLearningScreen.tsx#L1-L1325) ⭐ 가장 큰 컴포넌트
- [FeedbackScreen.tsx:1-387](src/components/FeedbackScreen.tsx#L1-L387)

**학습 데이터 구조**:

**공통 문장 챕터**:
- **초급** (4개): 기본 인사·상태, 요청·부탁, 확인·동의, 감사·사과
- **중급** (4개): 안내·설명, 제안·권유, 문제상황 대응, 일정·합의
- **고급** (4개): 완곡·배려 표현, 컴플레인 대응, 협상·설득, 비상·리스크

**직무별 문장** (주방보조 예시):
- **초급** (3개): 조리 도구, 식재료, 기본 조리 동작
- **중급** (3개): 주문 확인, 조리 지시, 위생 관리

---

### 3. AI 대화 연습 플로우

```
HomeScreen
  → ScenarioSelectScreen (시나리오 선택)
    → ConversationSetupScreen (설정)
      → ConversationScreen (대화)
        → FeedbackScreen (평가)
```

**관련 파일**:
- [ScenarioSelectScreen.tsx:1-137](src/components/ScenarioSelectScreen.tsx#L1-L137)
- [ConversationSetupScreen.tsx:1-146](src/components/ConversationSetupScreen.tsx#L1-L146)
- [ConversationScreen.tsx:1-224](src/components/ConversationScreen.tsx#L1-L224)

---

### 4. 학습 진도 추적

```
HomeScreen (최근 기록)
  ↓
ProgressScreen (상세 통계)
  ↓
StudyCalendar (캘린더)
```

**관련 파일**:
- [ProgressScreen.tsx:1-415](src/components/ProgressScreen.tsx#L1-L415)
- [StudyCalendar.tsx](src/components/StudyCalendar.tsx)

---

### 5. 커뮤니티 기능

```
CommunityScreen (게시물 목록)
  → PostDetailScreen (게시물 상세)
  → PostCreateScreen (게시물 작성)
MyPageScreen
  → MyPostsScreen (내 게시물)
  → MyCommentsScreen (내 댓글)
```

**관련 파일**:
- [CommunityScreen.tsx:1-248](src/components/CommunityScreen.tsx#L1-L248)
- [PostDetailScreen.tsx:1-180](src/components/PostDetailScreen.tsx#L1-L180)
- [PostCreateScreen.tsx:1-110](src/components/PostCreateScreen.tsx#L1-L110)
- [MyPageScreen.tsx:1-281](src/components/MyPageScreen.tsx#L1-L281)

---

## 🔧 주요 명령어

### 개발

```bash
# 의존성 설치
npm install

# 개발 서버 실행 (http://localhost:5173)
npm run dev
```

### 빌드

```bash
# 프로덕션 빌드 (dist/ 생성)
npm run build

# 빌드 결과 미리보기
npm run preview
```

### 코드 품질

```bash
# ESLint 검사
npm run lint
```

---

## 📊 코드 통계

| 항목 | 수량 |
|------|------|
| **화면 컴포넌트** | 23개 |
| **UI 컴포넌트** | 48개 |
| **Shared 컴포넌트** | 7개 |
| **Layout 컴포넌트** | 1개 |
| **전체 화면 컴포넌트 총 줄 수** | 6,144줄 |
| **가장 큰 컴포넌트** | SentenceLearningScreen (1,325줄) |
| **타입 정의** | 209줄 |
| **상수 정의** | 67줄 |
| **전체 구성 요소** | 79개+ |

---

## 🎯 코드 품질 및 구조 특징

### ✅ 장점

1. **명확한 컴포넌트 분리** - 각 화면이 독립적인 컴포넌트
2. **재사용 가능한 UI 컴포넌트** - 48개의 Radix UI 기반 컴포넌트
3. **타입 안전성** - TypeScript로 모든 인터페이스 정의
4. **일관된 스타일** - Tailwind CSS로 균일한 디자인
5. **확장성** - 새로운 화면 추가 용이

### 🔄 개선 기회

1. **상태 관리** - Redux/Zustand/Context API로 전환 권장
2. **라우팅** - React Router 도입
3. **API 통합** - 현재 Mock 데이터 → 실제 백엔드 연동
4. **테스트** - 단위 테스트/통합 테스트 추가
5. **성능 최적화** - 큰 컴포넌트 ([SentenceLearningScreen.tsx](src/components/SentenceLearningScreen.tsx)) 분할

---

## 📚 추가 문서

프로젝트의 다른 문서들:

- [커밋규칙.md](커밋규칙.md) - Git 커밋 규칙
- [역할분담.md](역할분담.md) - 팀 역할 분담
- [2025-10-25-수정사항.md](2025-10-25-수정사항.md) - 최근 수정 사항
- [src/REFACTORING_PLAN.md](../src/REFACTORING_PLAN.md) - 리팩토링 계획
- [src/Attributions.md](../src/Attributions.md) - 라이선스/기여자 정보

---

**문서 최종 업데이트**: 2025-10-25
**작성자**: Front_KoreanForYou 개발팀
